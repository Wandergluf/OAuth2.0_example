var nf = nf || {};
nf.math = {};

nf.math.sum = function(x, y) {
    //console.log(typeof(x), typeof(y));
    //console.log(x, y);
    return x + y;
};
nf.math.sub = function(x, y) {
    return x - y;
}
nf.m